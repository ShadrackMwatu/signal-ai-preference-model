# Deprecated Folder Notice

This folder is retained for compatibility or historical development. New CGE development should use `signal_cge/` unless otherwise documented.

The folder remains active for guarded SML execution workflows used by the app and tests. Execution responsibilities should migrate gradually into `signal_cge/` and `signal_sml/`.
