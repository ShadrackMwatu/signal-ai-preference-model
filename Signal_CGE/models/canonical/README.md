# Canonical Signal CGE Models

This folder stores lightweight, version-controlled Signal CGE model references.

Current contents:

- `signal_cge_master/model_profile.yaml`
- `templates/`
- `calibration_profiles/`
- `experiment_templates/`

Large runtime model artifacts belong in the ignored local workspace:

```text
models/Model/
```
