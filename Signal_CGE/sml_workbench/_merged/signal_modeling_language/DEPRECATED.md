# Deprecated Folder Notice

This folder is retained for compatibility or historical development. New SML development should use `sml_workbench/` for current app behavior and `signal_sml/` for future canonical migration unless otherwise documented.

Parser, schema, validator, and example behavior should be migrated with tests before this folder is retired.
