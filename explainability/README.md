# Explainability

This folder marks explainability as a dedicated platform layer inside Signal.

The active implementation currently lives in:

- `explainability.py`

It provides driver summaries, policy notes, and structured interpretation outputs for the main dashboard.
