# Codex Modeling Guide

When assisting Signal modeling work, first inspect the SAM, account mappings, closure assumptions, scenario templates, and diagnostics. Do not hard-code results. Keep assumptions visible. Run Python tests after modifying workbench code. If GAMS is unavailable, report that clearly and use the Python SAM multiplier fallback.
