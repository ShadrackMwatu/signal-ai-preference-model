# AI-CGE Workflow

Signal uses AI assistance to translate policy prompts into structured scenarios, run diagnostics, execute available model backends, explain results, and generate policy briefs. The economic model remains transparent and reproducible.

Chat prompts become scenario objects with shock type, target accounts, shock size, units, closure assumptions, and expected outputs. Every run writes timestamped logs under `cge_workbench/outputs/`.
