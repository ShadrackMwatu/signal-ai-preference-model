# Open-Source Replication Plan

Signal can be replicated progressively in Python and R by documenting SAM transformations, calibration formulas, closure rules, equations, shocks, and reporting outputs. The current Python SAM multiplier provides an auditable fallback. Future work should add equation-based open-source CGE modules, solver adapters, calibration notebooks, and cross-checks against the GAMS model.
