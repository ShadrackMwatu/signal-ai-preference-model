# Tests

Tests cover CSV loading, model training/persistence, FastAPI endpoints,
research-grade CGE/SAM export outputs, modular feature engineering, privacy
filtering, demand model training, prediction outputs, KMeans clustering,
opportunity generation, drift detection, retraining decisions, and no
individual-level data leakage.
They use Python's built-in `unittest` runner.
