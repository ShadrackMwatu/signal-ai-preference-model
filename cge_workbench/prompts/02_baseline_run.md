# Baseline Execution Prompt

Run the baseline Signal CGE Workbench workflow. Validate the SAM, account classifications, closure, calibration, and baseline consistency. Save diagnostics and logs in timestamped `outputs/` folders. Report any GAMS availability issue and use the Python SAM fallback when needed.
